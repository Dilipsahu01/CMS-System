import Page, { generateMetadata, generateStaticParams } from './[pageSlug]/page'

// Re-export the functions from the dynamic route
export { generateMetadata, generateStaticParams }
export default Page
